default:
all_metrics:
  vaultkv.created_on:
    enabled: true
  vaultkv.metadata:
    enabled: true
  vaultkv.metadata.error:
    enabled: true
no_metrics:
  vaultkv.created_on:
    enabled: false
  vaultkv.metadata:
    enabled: false
  vaultkv.metadata.error:
    enabled: false
